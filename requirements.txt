opencv-python>=4.8.0
numpy>=1.24.0

# Audio recording (choose one or both)
sounddevice>=0.4.6
# pyaudio>=0.2.11  # Alternative to sounddevice, uncomment if preferred

# Audio feature extraction (optional but recommended for ML)
librosa>=0.10.0
